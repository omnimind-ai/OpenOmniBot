{
  "projectId": "bench-notes",
  "name": "Notes",
  "entityName": "Note",
  "apis": [
    {
      "toolId": "note.create",
      "displayName": "Add note",
      "inputSchema": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string"
          }
        },
        "required": [
          "title"
        ]
      },
      "outputSchema": {
        "type": "object"
      },
      "run": {
        "use": "native.collection.create"
      }
    }
  ]
}
